import { Router } from "express";

const router = Router();

router.get("/available-regions",()=>{

})

router.get("/available-hardware-profile")

router.get("/available-elastic-versions")

router.post("/create-deployment")

